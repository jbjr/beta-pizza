
This is just used to hold things that are removed that may be added back

<TextField onChange={handleQtyChange} value={quantity} sx={{ml: 2, width: 50}} variant={"outlined"} label={"qty"}></TextField>

<DeleteForeverIcon sx={{color: '#ad1f1f'}}/>

<CheckoutHeader user={user.id} orderTotal={total}/>

onClick={updateInventoryOnPayment}
